<%- include("header")-%>

<body>

  <div class="box" id="heading">
    <h1><%= pageTitle%></h1>
  </div>

  <div class="box">

    <% tasksAdded.forEach(function(task){%>  <!--the percentage angular brackets are for anywhere in ejs thats code an not html-->
      <form class="" action="/delete" method="post">
       <div class="item">
        <input type="checkbox" name= "checkbox" value="<%= task._id%>" onChange="this.form.submit()">
        <p><%=task.name%></p>
      </div>
      <input type="hidden" name="listName" value="<%= pageTitle%>"></input>
      </form>
      <%})%>

    <!-- <% for(var i=0; i<tasksAdded.length; i++){%>
    <div class="item">
      <input type="checkbox">
      <p><%= tasksAdded[i].name%></p>
    </div>
    <%}%> %> -->

    <form class="item" action="/" method="post">
      <input placeholder="New task" type="text" name="task" autocomplete="off">
      <!-- <label for=""></label> -->
      <button type="submit" name="button" value=<%= pageTitle%>>+</button>

    </form>
  </div>

  <%- include("footer")-%>
